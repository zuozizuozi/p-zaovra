# 两次实机任务后的执行与验收修复方案

日期：2026-09-15。以下保留原审计与五步方案；实施结果及验证边界见文末。不能把计划中的验收项目自动视为已经验证。

## 结论与责任

造物改进.txt 将 Cline 定位为任务理解和验证闭环参考，将 Codex 定位为执行工程参考。codex-runtime-review.md 记录的实现包括共享 Shell 生命周期、取消、超时和输出保留；preflight-completion-review.md 中的 build/test/lint + snapshot 完成状态是 Zaovra 的本地设计，不能称为完整移植了任一竞品的验收机制。

已有机制有实际价值，但之前以组件测试和实现清单推断产品链路可用，结论超过证据。BYOK 通用性要求明确工具契约、保留真实执行证据和保守报告完成范围，不要求模型永不出错。

## 本次证据

- Windows 默认 shellCommand 选择 COMSPEC/cmd，PowerShell 单独采用 EncodedCommand。使用相同 Node 子进程 shell 调用形式复现：单行 node -e 输出 CHECK_RAN、退出 0；引号内包含换行的版本 stdout/stderr 为空、退出 0。该复现验证底层边界，实施时必须加入 AppProcess 和 V2 Bash 的整链路回归。
- 当前 Windows Shell 两项测试和 outcome 三项测试全部通过。多行场景只覆盖 PowerShell，cmd 只覆盖单行 type，未覆盖事故形态。
- system-context/builtins.ts 的环境基线上报平台和目录，没有实际选定的 Shell。Bash 描述说明配置/default，但没有随有效配置注入具体 Shell。
- outcome.ts 固定要求 build/test/lint，按 kind 覆盖检查记录，依据 exit 和 Location snapshot 判定。不能表达单文件游戏的语法、启动、交互检查，不能用项目快照证明桌面其他目录里的成品未变。
- 模型最终文字未强制绑定 outcome 的证据摘要。模型声称成功、todo 勾选、软件 completed_verified 是三种不同事实。两次事故不能据此断言软件当时给出了 completed_verified。
- 重复保护只统计连续、参数完全相同的工具调用。改引号或插入一次其他命令即可重置；这解释了为什么相近的失败验证可以反复发生，但不能凭无输出本身断定没有进展。
- 第一轮坏 JSON 留下 pending 并显示 interrupted 是历史已见事故。当前 runner 已有 failUnsettledTools，必须复现原始协议事件与退出路径再修，不能假定没有清理代码，也不能简单把所有 unknown 改成 failed。
- 第二轮成品最终可运行，暂停按住时逐帧切换。此前的语法错误和暂停问题是生成代码缺陷；执行层需提供有效验证手段并准确说明未验证范围。

## 顺序实施与验收

### 1. 修 Shell 契约和实际执行（P0）

位置：core/process.ts、core/tool/bash.ts、core/system-context/builtins.ts、core/test/process/windows-shell.test.ts。

统一解析有效 Shell，执行器、工具描述、环境上下文读取同一结果。Windows 未显式配置时优先选择可用 PowerShell 并使用现有编码调用；保留用户明确配置的 cmd，不暗中翻译为另一种语言。对 cmd 无法可靠承载的跨行引号输入明确拒绝并提供写脚本文件的纠正信息；合法多行脚本采用经测试的脚本文件调用方式，不能粗暴拼接换行。

验收：单/多行、中文和空格目录、单双引号、失败退出、无输出成功、超时和子进程清理通过真实 AppProcess 和 V2 Bash。检查预期副作用及输出，不仅检查 exit 0。事故输入不能再静默成功。无输出本身不判失败。

### 2. 修工具输入失败到最终状态的结算（P0）

位置：llm/protocols/utils/tool-stream.ts、core/session/runner/publish-llm-event.ts、runner/llm.ts、session/outcome.ts。

复放原始坏 JSON，追踪 input-start、参数解析、provider-error/Effect failure、Tool.Failed、Step.Failed 的路径。区分未执行的参数错误和已经开始、结果不确定的副作用。参数错误写终态及模型可读错误；允许有界纠正，不修猜 JSON，不自动重放有副作用的调用。保留原始参数/错误供诊断并限制展示长度。

验收：坏 JSON 不写文件、不留 pending，不误显示人工中断；正常用户取消仍 interrupted；真实崩溃仍 outcome unknown；恢复前核对现场，不能重复执行上次写入。

### 3. 让验收证据与本次成品对应（P0）

位置：schema/session-outcome.ts、core/tool/bash.ts、core/session/outcome.ts、core/snapshot.ts 的调用边界。

沿用现有检查记录，不另建 reviewer。记录检查对象、命令、退出结果、Evidence ID、产物版本/指纹及验证范围。项目任务根据实际脚本选择检查；HTML 游戏选择语法、启动、交互等适用检查，不能强迫所有任务凑 build/test/lint。按检查身份和对象保留结果，避免同种类的一次通过遮掉另一个对象的失败。外部目录成品需对应文件指纹；不能由无关项目快照背书。工具通过仅代表明确检查范围，不代表全部用户要求满足。

验收：语法错 HTML 不通过；修正后语法检查通过但未交互时仍明确未验证玩法；产物随后变更使旧检查失效；非 Git/外部目录有明确证据或保持未验证。输出带 ERROR 不一律失败、空输出不一律失败，进程测试需有可信断言；捕获异常并 exit 0 的坏测试不能靠字符串猜测修复。

### 4. 让最终报告和失败恢复使用同一证据（P1）

位置：core/session/runner/llm.ts、to-llm-message.ts、app-ui 的结果面板。

将紧凑的已执行检查/失败/未验证范围提供给模型，最终 UI 固定显示宿主记录的状态和证据，不让自由文本或 todo 覆盖它。不篡改模型原话，不承诺自动识别所有夸大表述。

保留现有步数上限和相同调用保护。先针对连续明确失败、明确未执行的验证增加有界纠正提示，要求换方法或报告阻碍；没有充分证据不把普通静默命令判成无进展。达到预算或已知硬限额时停止并保留状态，不继续自动重试同一请求。保留用户主动继续入口。

验收：模型说“全部通过”而证据缺失时，界面仍显示未验证；重复已知失败不会无提示耗尽轮次；正常多次修改测试不被误杀；修复失败必须保留原任务和已完成工作。

### 5. 以事故链路作为交付门槛（P1）

低层测试之外，在独立环境中从桌面 V2 消息入口走协议事件、工具执行、持久化和 UI。复放已捕获的模型响应可确定性验证软件，不消耗模型额度；真实模型测试另行标注，不能互相替代。

必测：多行检查静默返回 0、坏 JSON、错误吞掉却 exit 0、外部目录产物、验证后再改文件、免费额度耗尽、重启无重放，以及开头流式列表不闪烁。涉及公共 schema/API 时按仓库规则生成客户端；涉及界面时使用生产构建，保留修改前后指标。每项提交同时说明实现范围、验收证据和剩余边界。

## 范围约束

不引入强制第二模型、Computer Use、统一复杂评分器或大规模 Runtime 重构。浏览器/交互验收使用现有可用能力；缺少能力就如实标未验证。不能把本方案当作模型输出质量保证，优先保证执行真实、失败可解释、完成状态有证据。

## 本次实施结果

1. Shell：统一有效 Shell 解析，Windows 默认优先 PowerShell，环境上下文告诉模型具体 Shell。显式 cmd 的跨行命令在 AppProcess.run 执行前拒绝，要求改用脚本文件；未翻译命令。真实进程测试覆盖事故输入、Unicode/空格、空输出、退出码和超时清理，真实 Bash 注册链路验证脚本结果。
2. 工具结算：提供方流失败后，将未执行的本地 input-only 工具结算为 error，持久化回放后不会残留 pending；用户中断和未知副作用仍保留原语义。坏 JSON 直接停止并允许用户核对后继续，本次没有增加自动 JSON 修补或自动重放。
3. 验证记录：新增 syntax/smoke/interaction，保存 cwd、明确目标及 SHA-256 指纹，面板显示目标与 Evidence ID。记录按命令、种类和输入范围区分；独立 HTML 要求三个检查种类且目标仍匹配。普通项目读取已有 package.json 脚本；未识别的验收范围保留未验证。缺失、不可读或大于 64 MiB 的文件不能作为有效指纹证据。
4. 报告及停止：给模型提供已执行检查记录，并明确其尚未重新校验文件新鲜度；宿主面板独立显示完成状态和缺项。连续三次明确 Shell 失败后要求改变方法，四次后禁用工具并进入最终报告；成功检查、读取或编辑可解除计数。OpenRouter 每日免费额度耗尽不再自动重试，普通临时限流保留既有重试。
5. 回归：已运行真实 Windows 进程/Bash、V2 runner 持久化与恢复、原始 OpenAI Chat 事件解析、验证状态、Evidence 读取及生产构建下的 UI 回归。客户端与旧 SDK 已按要求生成，Core/LLM/Schema/Client/App UI 类型检查通过。

### 验证记录与范围

- Core：139 项通过，覆盖九个测试文件，包括取消/恢复、失败工具结算及变化参数不能绕过四次失败限制。
- LLM：48 项通过，覆盖 OpenAI Chat、tool-stream 和 executor；坏 JSON 不产生可执行 tool-call，硬额度错误不重试。
- UI：3 项通过，覆盖启动阶段流式布局、格式化列表保留、模型自称全通过但宿主仍显示未验证。
- 生产 UI 性能，同机 CPU 4 倍降速、40 个增量、12 轮历史：完成 10560.9 → 10602.4 ms（约 +0.4%）；首内容 261.5 → 243.2 ms；P95 帧间隔 16.7 → 16.8 ms。前后空白采样和滚动漂移为零，消息与 Markdown 节点未重建。仅一次前后样本，且基线可能受其他检查负载影响，不能宣称细微性能改善或排除所有回退。

### 尚未证明的边界

- 这是分层串联覆盖，不是一次从真实 Electron 输入框经真实 HTTP 提供方、Shell、数据库到 UI 的完整自动化运行；没有使用用户 Key 或消耗额度，也没有重启用户应用。
- 检查标签由 Agent 提供，宿主记录实际命令、退出码和目标版本，不会判断任意脚本是否真正覆盖玩法。吞异常后 exit 0 的脚本仍可能提供薄弱证据；不能据此宣布全部用户要求通过。
- HTML 的三类检查规则适用于显式声明目标的独立页面，不是任意语言、混合项目或全部产物依赖的通用验收规划器。未列出的依赖文件不受单文件指纹保护。
- 没有自动修改用户游戏，也没有新增浏览器控制能力或第二模型 reviewer。缺少实际交互检查时应明确保留未验证；人工实机仍是下一道验收。

## 2026-09-16：基础执行稳定性专项（OpenClaude）

本轮以当前 `main` / `80bd125d74f756dcdc6b8b3dfd60c0561898f6ee` 为基线，开始时工作区干净；没有切换分支、提交、推送或打安装包。参考 OpenClaude `2d7aa9c8415aa0413c3089bf36354f73e9108c0d` 的行为边界，独立实现，没有复制源码。

### 已复现并修正

1. **失败传递断裂。** 输出长度限制、无结束标记等路径已经持久化失败，Session 却可能正常返回，Work 因而进入成功/验证流程。先让旧测试断言执行失败，复现后由现有 `LLMError` 贯穿返回路径。Work 不再自动重跑本地执行异常，包括无法确定副作用的内部缺陷；保留失败尝试和阻塞原因。认证修复、显式重规划和验证失败后的既有修复路径仍保留。
2. **取消和工具结算。** 取消部分输出曾被记作普通错误；现在保留 `interrupted`，取消排队工具，不再继续下一轮。提供方失败时，已开始的工具如实结算，尚未开始的工具不再启动。不执行截断的参数，不将已有工具结果重放。
3. **超时归属。** HTTP 请求建立由 executor 管理，单次 120 秒、最多两次重试；响应头之后才启动网络静默 120 秒和有效帧等待 180 秒。SSE 保活不等于有效帧。错误响应体读取另限 10 秒；长于本地自动等待预算的 `Retry-After` 直接保留失败，不提前重试。非 HTTP 传输保留既有兜底，没有宣称完成 WebSocket 全链路审计。
4. **有限恢复和可见状态。** 纯文本达到输出上限，只允许一次保留历史的续写，并禁用该续写轮的工具；不完整工具参数不恢复。既有一次上下文溢出压缩恢复保留。复用 `SessionEvent.Retried` 显示重试；首个请求在等待模型时，不再误标为“等待接入当前任务”。没有新增执行循环、状态枚举或管理层。
5. **可关联诊断。** 记录请求开始、响应头、首帧、结束、重试次数/延迟、错误类别，以及 Session、模型、请求 ID 和耗时；不记录 Authorization、API Key 或完整请求正文。实际桌面已经重建，初次新后台为 `127.0.0.1:51601`，首批测试日志 run=`dc997765`。

涉及生产代码：Core 的 `session/runner/llm.ts`、`publish-llm-event.ts`、`work/runner.ts`；LLM 的 `route/executor.ts`、`route/transport/http.ts`；App UI 的 `context/server-session.ts`、`pages/session/timeline/message-timeline.tsx`。其余改动为故障回归、生产界面测试和本记录。

### 本地验证

- `packages/llm`：`bun typecheck`；`bun test --timeout 30000`，307 通过、30 跳过、0 失败。跳过项不是实机通过。包含认证/硬额度不重试、503 有界恢复、Retry-After、无首帧、保活无进展、空响应、协议/工具输入异常、流取消及慢下游消费。
- `packages/core`：`bun typecheck`；`bun test test/session-runner.test.ts test/session-run-coordinator.test.ts test/session-outcome.test.ts test/session-compaction.test.ts test/work-runner.test.ts --timeout 30000`，156 通过、0 失败。包含失败返回、Work 不假成功、不重放未知执行、排队工具取消、部分输出中断、一次纯文本续写、压缩和步数限制。
- `packages/app-ui`：`bun typecheck`；`bun test src/context/server-session.test.ts --timeout 30000`，22 通过。生产 Playwright 覆盖重试提示、首条请求状态和流式列表稳定性。
- 生产性能基线：CPU 30 倍降速、320 轮历史、160 个增量、batch=1，完成 99496.5 ms，P95 帧间隔 233.4 ms。第一次修改后 100702.7 ms；任务同时运行时再测 109726 ms，P95 300 ms。各次空白采样、滚动漂移均为 0，行和 Markdown 节点没有重建。不能由单次基准排除性能回退；最终空闲复测另记。

### 实机证据与判定规则

固定用户原有 `openrouter / nex-agi/nex-n2.5-pro:free`，OpenAI Chat / SSE，默认变体；没有切换模型、网关或收费接入。通过真实 Electron 输入框发送，每条最多等待 5 分钟。A/B/C/D 各三条，另测命令运行中取消和同会话新指令。写入仅在 `C:/Users/Administrator/Desktop/商单测试/zaovra-stability-20260916` 内。

完整测试记录保存在该目录的 `stability-evidence.json`，脱敏请求阶段日志在 `provider-diagnostics.log`。桌面数据库中的会话和工具 Evidence ID 可交叉核对。纯回复、已知内容、实际文件、进程退出码分别核验；`completed_unverified` 不被提升为工程验收通过。

已经确认的失败样本：B1 `ses_f59f4da70ffe9fIizoWpI5FbAl`，读取在 8 ms 内完成，第二轮请求 10854 ms 收到 HTTP 200，随后 180015 ms 没有有效帧，最终 failed，未重放工具。请求 ID 为 `gen-1789492172-Weq8yPCiG0xweDZkc9DS`。用真实历史经现有消息转换/请求构造核对，工具 ID 和读取内容对应正确；B2/B3 同链路成功。证据可定位至响应等待阶段，但不足以进一步区分 OpenRouter 路由、上游排队和网络/代理原因。

D1 `ses_f59e81b9bffeQ1foZeC3yERS2Q` 超过本次 5 分钟测试预算，由测试通过桌面 Stop 主动取消；未执行完修改/验证，计为未通过。该事件不是软件自动超时，不能与 B1 混为同一错误。已观察到工具参数中断后未执行，源文件仍保留原始错误。

**正常任务为 10/12，通过条件未达标。** 成功样本的宿主状态保持 `completed_unverified`，外部冒烟检查通过不改变工程验收状态。

| 类型 | 第一次 | 第二次 | 第三次 |
| --- | --- | --- | --- |
| A 只回复指定短句 | 通过，59.7 s | 通过，40.5 s | 通过，64.7 s |
| B 读取已知文件 | 失败，219.8 s，无首帧 | 通过，44.6 s | 通过，46.5 s |
| C 写入并读回 | 通过，183.4 s | 通过，50.6 s | 通过，195.7 s |
| D 修改并检查 | 未通过，300.3 s，测试主动取消 | 通过，207.7 s | 通过，270.1 s |

D2/D3 的 Agent 实际运行检查，退出码均为 0，分别输出 `CHECK-2-PASS` / `CHECK-3-PASS`；宿主再次独立运行同一检查也通过。D1 原文件未修复，独立检查失败，未用宿主代做任务来冒充 Agent 通过。

最终修正于 01:39 重建并启动，后台 `127.0.0.1:53137`，日志 run=`338f7e4e`。随后真实取消测试会话 `ses_f59d61406ffeDMP6zUAHvWpEIt`：Node 命令写入开始标记后点击 Stop，252 ms 内释放会话，状态 `interrupted`；等待超过原定 45 秒后没有结束标记。原会话新指令返回 `ZAOVRA-CANCEL-RECOVERED`，finish=`stop`，未继续旧命令。

取消测试之前有一次**测试脚本自身失败**：未等待新对话页面就发送，指令进入 D3 的既有会话，停止按钮并未被点击，探针自然运行结束。这次不计为产品取消通过或失败；记录在 `cancel-automation-attempt.json`，D3 后续消息也保留。补上页面就绪等待后重新进行上述有效取消测试。没有删除原正常任务失败样本。

最终空闲条件复测：先确认桌面后台没有活动会话，再运行相同生产配置。3 项 Playwright 测试通过；完成 101271.9 ms，相对基线约 +1.8%，首内容 174.3 ms，P95 帧间隔 250 ms。空白/漂移均为 0，行和 Markdown 未重建。之前约 +10% 的上升未在空闲复测中保持，但现有少量样本仍不能排除细微回退。测试任务已标注名称，原始失败和中断记录保留。

结论：已修复本轮复现的软件状态/结算缺陷，真实取消与后续输入通过；正常任务没有达到 12/12，**本轮稳定性验收未通过**。不得据本地回归或 10 个成功样本宣称整体稳定性修复完成。

### 尚存边界

- 当前接入延迟明显波动：仅短句回复三次约 60/41/65 秒。修复异常处理不等于已经修复上游延迟；不能声称当前模型接入已经稳定。
- HTTP 180 秒计时针对协议帧，尚不是理解内容的通用进度判定；连续合法空 JSON 帧、非 HTTP 传输的全部阶段，以及任意长任务的统一墙钟预算不在已证明范围。保留既有 provider-turn 上限、工具超时和 Work 预算。
- 静态核对发现的 OpenRouter `reasoning` / `reasoning_details` 兼容性缺口已在下述专项修复中处理。不能据此认定 B1 的无帧失败由它引起，也不能把专项成功样本扩展为所有 OpenRouter 模型均已验证。
- 对工具已产生副作用后的流失败，不自动重跑整轮。用户核对现场后才能决定继续；本轮未引入自动恢复执行、模型切换或第二模型 reviewer。

参考：[OpenClaude query](https://github.com/zuozizuozi/openclaude/blob/2d7aa9c8415aa0413c3089bf36354f73e9108c0d/src/query.ts)、[工具执行与取消](https://github.com/zuozizuozi/openclaude/blob/2d7aa9c8415aa0413c3089bf36354f73e9108c0d/src/services/tools/StreamingToolExecutor.ts)、[OpenRouter 流式说明](https://openrouter.ai/docs/api_reference/streaming)、[OpenRouter 思考字段说明](https://openrouter.ai/docs/guides/best-practices/reasoning-tokens)。借鉴的是有界恢复、明确终态和排队/已执行工具的区分；没有照搬其特性开关后的复杂恢复分支。

### 2026-09-16 OpenRouter 兼容链路专项修复

确认两个软件缺陷：OpenRouter 适配器直接复用普通 Chat 解析，丢弃 `reasoning` 与 `reasoning_details`；V2 模型解析还将 OpenRouter 指向通用兼容路由，绕过专用适配器。修复专用路由接线，在 OpenRouter 内接收文本与详细推理块，避免同一帧两种表示重复显示，按到达顺序保存详细块及签名/加密字段，并在下一轮 assistant 历史使用 `reasoning_details`（无详细块时用 `reasoning`）。不解密、改写或摘要详细块。普通 Chat/DeepSeek 的字段行为不变。没有新增执行循环、重试或放宽超时。

验证：四个新增场景对旧 OpenRouter 实现均失败，修复后通过。LLM 全套 311 通过、30 跳过、0 失败；Core 会话、协调、状态、压缩及 Work 回归 156 通过，模型路由回归 17 通过；两个包类型检查通过。持久化测试覆盖只有加密数据、没有文本的推理块经过事件投影重放后仍进入下一轮请求。路由测试覆盖默认/自定义地址、认证、参数和详细推理回传。未改 UI，本次不重复生产 UI 基准。

确认后台活动会话为空后，使用既有用户授权构建并重启开发桌面；实际产物时间 02:05，后台端口 54089，日志 run=2279c6dd。保持原 Key 和 `openrouter/nex-agi/nex-n2.5-pro:free`，从桌面新建会话 `ses_f59c0c94dffeiu3fNqcA0jBnqg`，仅读取已存在的 `known-2.txt`，正确回答 `READ-2-729`。一个 read 工具完成、两次 provider 请求均 200，无重试；两轮持久化历史都有 OpenRouter 推理详细字段，最终 `finish: stop` / `completed_unverified`，活动所有权释放。专项证据保留在原隔离目录的 `openrouter-repair-evidence.json`。

本次请求链总耗时约 101.2 秒：第一轮约 69.6 秒，第二轮约 31.5 秒。证明真实调用及推理历史链路恢复，但没有证明接入延迟已解决；历史 12 项验收仍为 10/12，未重写失败结果。B1 无协议帧超时的服务商、上游与网络责任仍不能仅凭此样本区分。

### 2026-09-16 响应延迟责任专项调查

未修改产品代码、配置、Key 或模型，也未重启桌面。原定同 Key、同模型交替测最小请求、桌面完整请求及工具历史回传。首个绕开 Zaovra 的 Node fetch 最小生成请求（190 字节，只要求回复固定短句）在 1031 ms 返回 HTTP 429：`free-models-per-day`，额度 50、剩余 0，服务端重置时间 2026-09-16 08:00（Asia/Shanghai）。立即停止后续生成请求，没有重试、切换模型或购买额度。因此最小请求与完整请求的 A/B **未完成**；不能将快速返回的 429 当作模型生成速度。

改用 OpenRouter 官方 `/api/v1/generation?id=...` 只读查询此前请求统计。5 个成功样本均显示 provider_name=Nex AGI，同一上游 endpoint；B1 的失败请求 ID 查询返回 404，无法据此确定其上游原因。元数据请求约 0.3～0.9 秒返回，仅说明当时该接口网络往返较快，不能证明生成路径完全没有网络影响。

| 样本 | 本地 provider 请求耗时 | 服务端 generation_time | 服务端 latency | provider_responses 中上游 latency | 原生输入 Token |
| --- | ---: | ---: | ---: | ---: | ---: |
| A1 固定短句 | 59.568 秒 | 58.324 秒 | 56.131 秒 | 55.499 秒 | 5959 |
| A2 固定短句 | 40.035 秒 | 38.477 秒 | 31.611 秒 | 31.337 秒 | 5959 |
| A3 固定短句 | 63.721 秒 | 63.137 秒 | 54.183 秒 | 53.806 秒 | 5959 |
| 修复后读取文件第一轮 | 69.586 秒 | 68.418 秒 | 37.784 秒 | 37.121 秒 | 5990 |
| 修复后根据读取结果回答 | 31.522 秒 | 30.873 秒 | 30.591 秒 | 30.562 秒 | 6186 |

以上服务端字段按原值展示，latency 与 generation_time 不是可相加的阶段。两轮读取任务服务端 generation_time 合计约 99.3 秒，与本地约 101.2 秒吻合；由此判断主要等待位于服务端处理/上游响应路径，非 Zaovra 本地工具或 UI 运算。上游 latency 占据服务端 latency 的绝大部分，证据进一步指向当前 Nex AGI 接入，尚不能区分上游排队、输入预处理与推理调度。不能据此断言 OpenRouter 全平台或所有模型都慢，也不能排除约 6000 输入 Token 的完整 Agent 请求增加了服务端负担。

原始统计保存在原隔离目录 `latency-provider-metadata.json`，受限的最小请求结果在 `latency-direct.json`。后续在同 Key 额度可用时补至少三组交替最小/完整请求，才能量化 Zaovra 请求负担；本轮未安排自动唤醒或后台重试。

接口依据：[OpenRouter 请求统计 API](https://openrouter.ai/docs/api/api-reference/generations/get-request-&-usage-metadata-for-a-generation)。结论依据账号实际返回的统计，非文档示例值。

### 2026-09-16 Luminous Garden 实际交付排查

本次以会话 `ses_f5798992effeFr6tshWacQqkni` 的已有作品为验收对象，未重写 `index.html`。修复六处实际衔接缺陷：

- 回合审查查询尚未取到数据时，不读取会触发上层 Suspense 的数据，避免隐藏整个聊天工作区。
- 审查使用真实用户消息，排除适配成 user 的 shell / synthetic 行，避免 `User turn not found`。
- 压缩记录保留 content 为空时的 structured 工具结果，避免摘要遗漏文件读取内容。
- OpenRouter 同索引、相同元信息、连续且无签名的 reasoning.text 增量合并为完整块；签名、加密和不连续块保持原顺序，不做摘要或删除。
- 后台 bash 默认不设期限，显式 timeout=0 仅允许后台；显式非零期限继续有效，会话停止仍清理后台进程。前台仍默认 120 秒。
- 已完成的模型任务与仍存活的后台预览分开计算 outcome；只有本进程确认持有的后台 shell 可以豁免“未知结果”，失去进程所有权后仍标记 interrupted / outcome unknown。

验证：Core 五个相关测试文件 142 通过；LLM 三个相关文件 39 通过；UI 两个状态/适配测试文件 32 通过；生产构建浏览器回归两项通过（延迟审查时保留聊天、后台 shell 不冒充用户回合）。随后补充真实后台进程 outcome 断言，bash-runtime + session-outcome 十项通过。Core、LLM、app-ui 类型检查通过。没有将这些测试数量视为真实模型交付通过。

生产 UI 压力场景为 320 历史回合、160 增量、审查开启、30 倍 CPU 降速。基线完成时间 172907.7ms；同时运行 3D 页面的首次复测 195753.5ms；关闭 3D 页面后的独立复测 169792.5ms，RAF P95 为 616.7 → 616.6ms，滚动漂移 0，空白采样 0。第一次上升存在干扰，独立复测未复现，不能据此宣称所有场景无回退。

真实结果分开记录：

1. Zaovra 使用原 OpenRouter 免费模型启动了预览，成功执行 syntax，并在 smoke 检查失败后停止、如实报告。该失败来自本次验收脚本错误地按大写标题匹配 HTML，已修为 HTTP 内容与磁盘原文件精确比较；没有修改作品来迎合断言。
2. 修正脚本和后台预览 outcome 后，最终模型请求立即收到每日免费额度 HTTP 429：limit=50、remaining=0，未自动反复重试。因此 **Zaovra 自主闭环验收仍未通过**，当前真实会话保留 failed，没有手工修改为 completed_verified。
3. 主机辅助交付已通过实际浏览器验收：页面与文件一致、WebGL 场景真实渲染、开关往返、拖拽改变相机位置、滚轮改变相机距离、窗口变化同步 canvas、无浏览器运行异常。当前预览由主机启动并保持在 `http://127.0.0.1:8000/`，已在 Zaovra 成果面板打开。不能将此辅助交付冒充 Agent 独立完成。

证据保存在 `quality/chain-audit/delivery-*`，作品目录保存 `verify-delivery.cjs`、`delivery-{syntax,smoke,interaction}.json` 及截图。原 HTML SHA256 为 `5cc1f790351ef7e27a9a134792bd3c6cadf9e98693cf625a463ccf4526ad5a28`，本轮结束时未变。后续有额度时只补既有作品的最终模型验收，不重新生成作品，不自动重放旧工具。

另外，开发模式内置 relaunch 在工作目录已切到 home 后复用相对参数 `.`，触发启动错误窗口；本次使用正常开发启动恢复，未把这一开发启动问题标为已修复。普通启动已成功加载最终后台修复。

### 2026-09-16 无模型额度的后续链路修复

用户确认：停止 Agent 时保留预览；归档时关闭该任务所属服务。浏览器能力范围限于本地网页验证。真实模型验收待额度可用后补做，本轮不调用模型、不探测额度、不切换付费模型。

本轮修复：

- Bash 增加可选 `background_kind: task | preview`，仅后台可用，默认 task。普通后台任务计入执行中并受停止控制；显式 preview 不阻止模型任务结束，停止保留，归档、删除、bash_job cancel 或所属进程退出时清理。旧的未标记命令保守视为 task，不从命令字符串猜测用途。bash_job 返回实际工作目录和类型，避免把 Location 目录误认为服务的工作目录。
- 归档先记录归档状态，随后等待执行与所属后台服务清理，并取消待执行输入。执行入口及每轮边界拒绝继续已归档会话；过期恢复按钮也不能重新恢复它。归档接口完成才代表清理结束。恢复的串行区内不调用自身 interrupt，避免等待自身造成死锁。普通继续可复用仍存活的显式预览，不自动重放旧工具。
- 前端停止不再提前清空任务清单，不再吞掉请求失败；同一任务的并发停止合并为一次请求，成功后才触发停止回调，失败显示错误并保留现场。
- 验收面板只在查询已取到数据后读取结果，避免不必要的 Suspense。该面板原本已有局部 Suspense，不能把这处防护描述为已证实的整页闪黑根因。
- 桌面预览的异步打开使用每次请求的独立身份，防止相同成果 ID 的旧打开请求覆盖新请求。真实 HTTP 测试覆盖不同项目独立端口、文件修改后读取新内容、关闭一个服务不影响另一个。

本地验收记录：Core 执行/停止/后台服务/恢复/结果 136 项，压缩/证据/输出保留/跨进程持久化 17 项；LLM 断流/超时/适配/重试 39 项；UI 提交/同步/适配 39 项；桌面预览 HTTP 4 项。浏览器检查涵盖停止失败、停止请求合并、挂起验收查询、挂起审查查询、shell 回合识别、等待与重试展示。测试使用本地临时目录、可控响应和本机进程，不消耗模型额度。

浏览器测试曾出现一次测试自身的路由匹配错误：outcome 请求带查询参数，字符串匹配未拦截到请求。改为匹配可选查询参数，并强制断言请求实际被拦截后，再检查页面可见；不能把未触发请求的测试算作覆盖。

未完成的真实验收仍单独保留：同一可用模型下，空目录生成、网页实际交互验证、成果交付、修改再验、停止继续；同时记录耗时、调用次数、重复操作及人工介入。当前本地测试不能证明模型会正确选择 preview 标记、能独立使用浏览器完成验收，也不能证明所有 GPU/原生窗口闪屏消失。没有新增通用 Computer Use 或自动恢复重放，也没有把手动辅助交付计作自主成功。

已在本任务设置后续提醒；无新的额度或验收进展时不重复催促，不自动发起真实请求。

### 2026-09-16 GLM-5 授权与真实链路复测

用户随后提供 GLM-5 凭据并授权实际调用，不再需要额度提醒；原提醒已暂停。只在内存中比较凭据文件和已保存配置，确认一致，不将密钥写入日志或仓库。接入是自定义 `api-edgecloudapp-com`，模型 `glm-5`，不是 OpenRouter。

- 短句任务 `ses_f56f35e6fffevAVc0yUc1D0Shu` 实际收到 READY。上游 HTTP 200，约 2.4 秒结束；旧前端仍显示思考，证明不能把这一现象归咎于 Key 或模型响应慢。
- 提交成功后主动等待执行结束，并在执行所有权释放后重新核对持久化消息，覆盖切页漏掉结束事件。新增“完全漏掉结束事件仍取到最终正文”的测试。智能体加载请求明确抛出错误、有限重试；发送时区分模型未选与智能体未加载，后者可点击重试，不再笼统暗示 Key 无效。原先初始化 503 的具体触发原因仍未完整复现，不能宣称所有启动故障根因都已解决。
- 重启加载后，真实任务 `ses_f56ea3886ffeKiVixbTQk2s4CJ` 在新的独立目录生成单文件点击游戏，自行运行浏览器检查并修改重置逻辑。人工仅提供任务和放行该目录权限，没有代写游戏。任务包含 43 次工具调用，约 8 分 12 秒（包含人工权限等待），显示 906532 Tokens；这不是可接受的效率基准，也不能将 Token 总量等同费用。
- 该任务暴露子任务默认模型回落到旧 OpenRouter 配置，收到 402。修复为新子任务在未指定模型、角色模型或命令模型时继承调用它的父模型；既有子任务保留原选择。14 项子任务/恢复测试通过，Core 类型检查通过；真实修复后子任务调用另记。
- 游戏任务最终正常恢复 Send 按钮并显示完整终稿，但模型声称“全部验证通过”与记录不符：存在失败交互检查、缺少 syntax。系统保持 failed，没有强制改为通过。因此这一轮只是生成及部分交互成功，**不是完整自主验收通过**。最终状态回补不能代替运行期间全部流式更新稳定性验证。

补充本地验证：智能体目录失败/恢复测试 7 通过；提交测试 7 通过；执行同步测试 23 通过；浏览器回归 6 通过；Core/app-ui 类型检查通过。复测原始记录位于忽略目录 `quality/chain-audit/glm-*`，不包含密钥。

随后重新构建并重启，真实子任务 `ses_task_ses_f56ea3886ffeKiVixbTQk2s4CJ_msg_0a924afeb001NtHeBqvCq78tSW_tool-5d03a1bbaac14155a4b102cea60c4294` 返回 CHILD_READY，持久化记录确认父子均为 `api-edgecloudapp-com/glm-5`，没有再切换旧 OpenRouter。

冷启动还观察到目录短暂陈旧：后台已经提供 glm-5，模型菜单刷新后才恢复。菜单打开时增加一次主动目录核对，复用现有加载与错误反馈，不新增循环轮询。这是恢复路径补强，尚未定位最初陈旧快照形成的全部条件，不能据此宣称配置链路已彻底稳定。

最终生产 UI 压力复测为 164818.1ms，RAF P95 600ms，空白采样 0、滚动漂移 0、行节点未替换；本轮修改前 186761ms / 666.7ms，中间复测 188093.2ms / 666.8ms。单次最终复测没有显示回退，不能当作普遍性能提升证据；最后菜单打开刷新改动不涉及时间线，另跑模型初始化/选择浏览器回归。

最后一次从桌面“新对话”发送真实短句，会话 `ses_f56d88841ffed6RE6NlJCpv4Ba` 收到 NEW_TASK_READY，模型记录为 glm-5，约 2.3 秒从用户消息到完成，UI 恢复 Send。没有再出现“请选择智能体和模型”的泛化错误。

模型菜单旧回归首次为 1 通过 / 4 失败：三项仍匹配不带“Own Key ·”前缀的旧按钮名称，但截图已显示正确模型；一项夹具把服务端已应被白名单过滤的 ghost 模型继续当可用模型返回。修正标签匹配与 V2 夹具契约后复测，未为了这些断言更改产品筛选行为。这些浏览器夹具不能代替服务端白名单测试。

该组第二次复测还有一项旧键盘顺序断言失败：有配置时搜索框之前已有“Model service”选择框。补充对这个焦点的断言后，最终 5/5 通过（21.6 秒），包括空目录、已有配置、延迟模型目录、选择持久化及两种布局的嵌套模型 ID。最终 app-ui 类型检查、diff 空白检查通过。真实游戏的完整验收、修改再验及真实停止继续仍未全部通过，后续不应跳过或用短句成功代替。

### 2026-09-17：稳定交付链路复验（GLM-5，自动选模延后）

本轮使用用户授权的真实额度，合计目标不超过 150 万 Tokens。仅使用已有 GLM-5 配置；没有擅自将 DeepSeek Key 发送到未经确认的服务地址，没有引入自动难度路由。原始证据在忽略目录 `quality/chain-audit/0917-*`，不记录密钥。

代码修复及依据：

- 找到冷启动目录的具体缺口：内置插件异步初始化，执行器等待 ready，模型/提供商/连接目录接口此前直接读取正在初始化的状态。四个目录读取复用已有 ready 标记（agent 使用 agent-ready，其余使用 internal-ready），等待上限 15 秒；初始化超时返回失败，不以 200 空目录伪装成功。当前超时为通用服务器错误，尚不是统一的面向用户诊断。服务端完整 location HTTP 回归 5/5、28 个断言通过；桌面重启后 GLM-5 直接显示。
- 固定系统上下文放到逐轮变化的验收提示之前；验收提示只附检查类型、退出码、调用 ID、目标路径，完整命令与日志仍在历史和证据中。未裁剪历史，未启用激进压缩，尚无缓存命中或整体 Token 节省的 A/B 结论。
- Windows PowerShell 上下文明确不支持 Bash 重定向和 `&&`，复杂嵌套命令优先写脚本。与 pwsh 区分，不对全部 Windows Shell 一概套用。
- 明确提示模型：仍存在非零验收记录时不能声称整轮验收通过；换另一条命令通过不抹掉原失败。保留现有保守状态计算，未按检查类型粗暴覆盖失败。

参考来源与实际采用范围：

- [Cline 会话历史与用量同步](https://github.com/cline/cline/blob/15f001ad0b1992965112fc3158f45ec6d2cb090d/apps/examples/desktop-app/webview/hooks/use-session-history.ts)：在途读取不能代表较新状态，需要在合适边界重读。
- [Cline 执行生命周期](https://github.com/cline/cline/blob/15f001ad0b1992965112fc3158f45ec6d2cb090d/sdk/packages/core/src/runtime/orchestration/session-runtime-orchestrator.ts)：保留完整上下文、明确取消和完成边界。没有复制其完整调度框架。
- [Codex App Server 协议](https://github.com/openai/codex/blob/main/codex-rs/app-server/README.md)：初始化和状态读取需有明确边界，通知触发重新读取，不把旧状态或失败读取解释成当前有效结果。复用 Zaovra 已有等待和回读机制。

真实测试记录：

1. `ses_f54e8c905ffeyM1bnHN0wHs9GF`：为已有游戏生成可重跑的 verify.js。模型先混用 .mjs 与 require，失败后改正；第一次最终说明仍过度声称完成，后台正确保留 failed。再次独立复验 syntax/smoke/interaction 均退出 0，目标文件指纹一致，后台 completed_verified，界面显示“所列检查已通过”。这一成功包含审查后的追加指导，不能称为一次自主完美交付。
2. `ses_f54df4d9bffenF7k0burtUG99T`：真实前台命令先等待 45 秒再写文件。00:51:17 点击 UI Stop，00:52:35 文件仍不存在、active 为空、状态 interrupted。随后点击“核对现场后继续”，只检查现场和证据，没有重新写文件；界面与后台为 completed_unverified，因为没有声明工程验收检查。中断请求缺少完整用量，预算单独预留余量，不能记为零成本。
3. `ses_f54d9ac14ffeT8XcId01f0g9LF`：为游戏添加空格键操作，模型修改成品与脚本并修复失败检查，最终三项检查通过、后台与 UI 一致。耗时约 3 分 18 秒、298915 Tokens，效率仍偏低。审查脚本后发现其长按检查未真正生成 repeat 事件，需要补测，不能把自动通过等同于完整需求验收。

期间还发现用量栏等待整个 execution 结束才刷新，导致后台已有多轮用量、UI 仍显示 0；这与用户的预算判断直接冲突，追加逐轮刷新修复及浏览器回归。真实工具/文字进度在后两次任务中实时显示；第一次观察到的短暂进度陈旧未稳定复现，不宣称所有流式与闪屏问题已根除。

本轮后续完成：

- 用量栏仅增加每次 step/compaction 结算后的回读，保留执行结束和窗口恢复时的兜底；不增加定时轮询。浏览器回归先复现失败（在途 execution 下期望 250、实际仍为 100），修复后验证 100→250→310→最终 495，以及仪表盘错误/恢复入口，1/1 通过。真实执行中后台与界面同时为 323021 Tokens，active 仍为 running，证据 `0917-live-usage.json`。
- 审查后追加要求模型补齐真实 repeat 事件：verify.js 连续三次 keyboard.down，记录并断言 `[false, true, true]`，断言重复按键前后分数一致。重新运行 syntax/smoke/interaction 后全部退出 0，成品及脚本指纹匹配，最终 completed_verified，UI 一致。该追加轮消耗 74666 Tokens，约 1 分 7 秒。三个测试对话已标为“GLM实测 · 成品验收 / 停止与继续 / 键盘交互再验”，位于“商单测试”。
- 本轮记录用量合计 526736 Tokens（input 509132、output 11231、reasoning 6373；提供商未报告缓存），另有一项中断请求用量未知；为预算保守预留 100000，预算占用 626736，低于 150 万。这不是供应商账单或费用估算，也没有将未知请求当作免费。
- Core 相关测试 116/116、421 断言通过；Server location HTTP 测试 5/5、28 断言通过；Core、Server、app-ui 类型检查通过，修改文件格式及 diff 空白检查通过。成品由 Zaovra 执行工具修改，外部审查提供了验收条件与覆盖反馈，没有代写产品实现。
- 尚存边界：独立一次生成与自主完整验收的成功率仍未建立；单项小改动约 30 万 Tokens，效率仍偏低；首次验收模型的完成措辞曾与保守状态冲突，新增提示不是形式化保证，系统仍以记录为准。日志出现 ResizeObserver 警告，未据此直接宣称已找到全部闪屏根因。此次不引入第二模型审查器、自动模型路由或激进压缩。

最终生产性能对比（相同 30 倍 CPU 限速、320 轮历史、160 段流式增量、审查面板打开）：修改前 179557.4ms / RAF P95 683.3ms；修改后 176069.1ms / RAF P95 633.3ms。前后空白采样均为 0、滚动漂移均为 0、行与 Markdown 节点均未替换。此单次对比未观察到回退，不能推断所有机器均提速或所有黑屏均已解决。原始日志为 `0917-usage-before-performance.log` 与 `0917-usage-after-performance.log`。
