# B1 连续压缩定向实测

状态：两项任务已自然结束，审计完成。CSV 全通过；库存未通过完整验收。新发现的摘要职责隔离问题已修复；后续 [B2 四份摘要重放](./compaction-b2-live-20260919.md) 均通过，B1 历史结果不改判。以下“待重放”描述保留 B1 结束时的状态，最新结果以 B2 为准。

## 固定设计

- 模型：deepseek-v4-flash-0731，沿用已有连接与 A 的软输出额度，初始 16,384，最大 65,536。
- 任务：库存、CSV 各一次，沿用 C3 种子和独立判题，包括库存非枚举自有字符串键与 CSV BOM 补测。增加原测试文件字节不变的独立检查。
- 流程：原始任务＋只读规划 → 手动压缩 → 预登记修正＋只读确认 → 手动压缩 → 明确解除只读限制并实施。阶段指令全部在首个请求之前写入注册表，不临时提示模型。
- 两次压缩由测试驱动明确触发，测试宿主的 keep.tokens=128，自动压缩仍开启。生产配置未改。不能把这里的成功归因于自然触发时机或宣称自动超限路径已真实覆盖。
- 检查后续每个正常 Agent 请求是否包含宿主从持久记录读出的完整原文与修正；检查规划/确认阶段的文件是否不变、是否调用禁止的执行工具；检查两次压缩实际完成及最终代码独立验收。

## 冻结与证据

- 证据：`F:/p-zaovra-cline-kernel-poc/experiments/pixel-courier-ab/.evidence/compaction-b1-1789741259281`
- 工作目录：`C:/Users/Administrator/AppData/Local/Temp/zaovra-compaction-b1-1789741259281`
- 驱动：`compaction-b1-host.mjs`；审计：`audit-compaction-b1.mjs`。
- 注册六个产品包 src、驱动、审计和判题依赖的哈希；测试中不修改这些文件。每阶段保存会话与结论，独立判题前复制提交文件；原始结果不回写。
- 已启动的完整分阶段任务不会因达到 Token 参考值被打断；达到额度后不再启动下一任务。失败请求未返回 usage 时单列未知，不能当作 0。

## 判定口径

机制通过要求两次压缩均真正完成、后续请求原文和修正保留、只读阶段没有改文件或调用禁止工具。交付通过另要求独立判题通过、原测试字节未改、宿主正确完成。任一侧失败都不能合并成“B 全部通过”。

强制超限与取消以本地可控注入结果为证据，本轮不把它们冒充自然实测。样本少且只用一个模型，不推断所有模型稳定。

## 实测结果

| 任务 | 宿主结果 | 独立产物检查 | 两次登记压缩 | 请求数 | 总 Token | 耗时 |
| --- | --- | --- | --- | ---: | ---: | ---: |
| 库存 | completed_unverified | 通过，含非枚举键补测 | 失败、成功 | 36 | 1,372,911 | 412.344 秒 |
| CSV | completed_verified | 通过，含 BOM 补测 | 成功、成功 | 38 | 1,613,848 | 688.617 秒 |

两项原测试文件均字节未变。实施阶段 51 次正常请求（库存 24、CSV 27）均带入完整原始任务和后续修正。两项均自然结束，无静默停止。坏摘要未覆盖旧历史，之后仍能压缩并继续实施。

库存首次压缩前的规划阶段调用了一次被阶段指令禁止的 bash（`echo probe`），没有改文件；修正确认阶段没有禁止调用。这是压缩前已经发生的指令遵守问题，不归因于压缩丢失，也不声称摘要修复解决它。最终库存还声明存在实现取舍及历史只读合规证据缺口，宿主保持未验证。有限独立检查通过不能替代完整验收，库存的机制与交付判定均为失败；CSV 两者均通过。

用量合计 **2,986,759**：输入 2,810,978（其中缓存 2,556,928），输出 175,781，未知用量 0。参考额度 1,500,000；库存结束时余 127,089，因此启动 CSV，并按约定让该完整任务自然结束，超出参考值 1,486,759。随后没有新任务或 API 调用。C 历史消耗不计入本轮。

真实运行结束时，六个产品包源码及测试仪器哈希均未变。`b-audit.json` 保留原始审计；`b-audit-final.json` 修正只读阶段的工具归属：首次压缩失败保留旧历史，原审计把规划期的同一次 echo 又计入修正期。派生审计只统计当前阶段用户消息之后的工具，未改原始记录、题目或通过规则。

## 实测后修复与剩余验证

库存第 8 次请求是摘要调用，正常 stop，使用 19,790 Token，却返回修复计划而非要求的摘要章节。原请求只有 user 消息，摘要职责和归档任务处于同一指令层级；证据符合职责混淆，不是输出截断。宿主正确拒绝了该摘要。

运行结束后，生产摘要请求改为独立 system 指令限定摘要职责，归档会话保留在 user 数据中；上下文估算同时计入 system 指令。没有增加重试、扩大摘要输出额度或放宽摘要校验。是否减少真实模型的职责混淆仍待重放确认。

修复后 Core 类型检查通过，8 个相关测试文件 **222 项通过、0 失败、840 次断言**。测试覆盖真实请求的角色隔离、原文保留、坏摘要拒绝、失败保留旧上下文、超限与 C 收尾回归。

已离线准备 4 份原摘要请求（库存 #8/#12、CSV #5/#11），使用生产请求构造器与实际传输编码，原 user 内容不变、system/user 分离、无工具、输出仍为 4,096。目录：`F:/p-zaovra-cline-kernel-poc/experiments/pixel-courier-ab/.evidence/compaction-b2-prepared-1789742539091`。离线准备网络请求 0、付费 Token 0。

拟另申请 **200,000 总 Token 参考额度**，仅重放这四份摘要并检查结构与事实保留（原四次合计 81,214 Token）。这不是完整任务重跑，也不能消除库存只读阶段违例或据此把 B1 改判为全通过。新额度批准前不发起真实重放。
