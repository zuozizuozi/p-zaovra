# B2：摘要职责隔离定向重放

状态：获批的四份摘要请求已完成，4/4 正常结束并通过生产摘要校验；本轮定向验证完成。没有重新执行完整库存或 CSV 任务，不改判 B1 历史结果。

## 范围与证据

- 用户批准新增 200,000 总 Token 参考额度，输入与输出均计入，缓存输入也计入；已开始请求正常跑完。
- 使用 B1 原模型 `deepseek-v4-flash-0731` 与已有连接；重放库存 #8/#12、CSV #5/#11，原始 user 内容逐字不变。使用此前通过生产请求构造器与传输编码器生成的请求，新增 system 摘要职责边界，发送的 `max_tokens` 仍为 4,096，无工具。
- 运行前登记请求哈希；运行后原始请求哈希、实际发送请求哈希、user 内容、摘要源码哈希均一致。无中途修改产品代码或模型返回工具执行。
- 成功重放证据：`F:/p-zaovra-cline-kernel-poc/experiments/pixel-courier-ab/.evidence/compaction-b2-live-1789750395126`。包含 registration、四份请求/原始 SSE/摘要、results 与 audit。
- 驱动：`packages/core/script/replay-compaction-live.ts`。它是离线准备请求的诊断发送器，不是生产恢复或 Agent 执行框架。

## 结果

| 原摘要请求 | 正常 stop / 完整流结束 | 生产结构校验 | 工具事件 | 总 Token | 耗时 |
| --- | --- | --- | ---: | ---: | ---: |
| 库存 #8（原失败） | 是 | 通过 | 0 | 20,595 | 30.739 秒 |
| 库存 #12 | 是 | 通过 | 0 | 26,327 | 10.710 秒 |
| CSV #5 | 是 | 通过 | 0 | 14,568 | 39.157 秒 |
| CSV #11 | 是 | 通过 | 0 | 21,983 | 10.847 秒 |

有效四次调用共 **83,473 Token**：输入 68,473、输出 15,000、缓存输入 0。除此之外有 **1 次连接失败，用量未知**；不能把它算作 0。B1 与 B2 已报告用量合计 3,070,232 Token，另有该未知项。

首次使用 Bun 传输时，建立连接失败，没有收到 HTTP 响应或 usage，驱动立即结束。证据保留在相邻 `compaction-b2-live-1789750306796` 目录。随后仅做 DNS/TCP 与无鉴权模型列表地址探测（无生成请求），确认 Node 可以连接。诊断发送器改用 Node，重新登记并完成四份冻结请求。即一次连接失败后的显式运输层重发，不是五次有效模型样本，不是生产自动重试。未改变 TLS 校验或凭据；凭据通过子进程标准输入传递，不写入命令参数或证据。

网关对 CSV #5 报告 completion_tokens=5,664，其中 reasoning_tokens=3,556；虽然发送值为 4,096，不能推断此网关以同样口径硬限制总输出。本轮按实际 usage 记账，未修改额度参数，响应正常结束。

## 内容复核

- 两份库存摘要均保留原始库存契约、只读阶段限制、尚未实施/测试的状态及早先 `echo probe` 越界事实；#12 保留原测试字节不变、新测试另建文件、所有自有字符串键及原子性的后续修正。
- 两份 CSV 摘要均保留解析/序列化与交付要求、只读阶段和验证尚未执行；#11 保留首字段 BOM、单空字段往返及原测试字节不变的修正。
- 此前失败的库存 #8 这次输出摘要，没有继续向用户回答修复计划；四份均未把计划写成已实施或已通过测试。
- 摘要仍有语义局限：规划期模型提出的待确认取舍仍被摘要保留；CSV 空字符串与两个引号的简写有歧义；摘要 head 在消息中段分割时会把所见尾部描述为“截断”，而尾段实际另存于 recent-context。不能把摘要当作事实裁判或整个会话的唯一记录。本轮未删除原文保护，也未用正则纠正语义。

以上为关键约束的人工内容复核，不是所有命题的形式化语义证明。一次失败样本重放通过支持修复方向，但没有同步旧版对照或多轮重复，不据此宣称失败率归零。

## 本地验证与收尾边界

最终 Core 类型检查通过；8 个相关测试文件 222 项通过、0 失败、840 次断言。覆盖原文保留、摘要职责、格式与工具拒绝、失败保留旧上下文、超限/取消及 C 收尾回归。未放宽 C 的验收标准。

B 的本轮定向修复与验证完成：要求由持久原文保留，摘要职责分离，坏摘要不覆盖历史。但 B1 库存完整验收仍失败，压缩前只读违例仍未修复；本轮不验证自动压缩触发时机、跨模型稳定率或修复后的整项库存交付。完整基线验收继续保持未通过，相关遗留留在 TODO。没有为这四份重放追加完整任务或继续消耗剩余额度。
