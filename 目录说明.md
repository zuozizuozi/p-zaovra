# 造物：官网与桌面端目录

这是同一个仓库中的多个独立应用。按下面的目录修改对应产品，公共代码集中维护。

| 目录 | 用途 | 原目录 |
| --- | --- | --- |
| `packages/website/app` | 官网首页、下载页、账户与订阅后台 | `packages/console/app` |
| `packages/website/core`、`function`、`resource`、`mail`、`support` | 官网的业务逻辑、服务、资源、邮件与支持工具 | `packages/console` 下的同名目录 |
| `packages/website-docs` | 文档站、文档落地页与分享页面 | `packages/web` |
| `packages/desktop-app` | Electron 桌面端：窗口、系统集成、安装包 | `packages/desktop` |
| `packages/app-ui` | 桌面端使用的工作台界面，也能作为浏览器应用运行 | `packages/app` |
| `packages/ui`、`packages/session-ui` | 公共界面组件 | 未移动 |
| `packages/core`、`schema`、`protocol`、`server`、`client`、`zaovra` 等 | 公共运行时、协议、客户端和命令行工具 | 未移动 |

## 日常操作

Windows 测试可直接双击对应目录唯一的 `启动器.bat`：

- `packages/desktop-app/启动器.bat`：拉起真实 Electron 桌面端；首次自动补装缺少的 Electron 运行程序。
  本地桌面开发模式暂时免账户登录，便于开发工作台功能和 UI；正式安装包仍保留账户校验。
- `packages/website/启动器.bat`：启动官网并打开 `http://127.0.0.1:3001`，使用现有 Vercel 资源适配配置。

启动器自动定位仓库，缺少依赖时使用冻结锁文件安装。保持命令窗口打开，使用 Ctrl+C 停止；报错时窗口会停留以便查看。

本地运行的是当前工作区代码，与公网发布使用同一套源码入口，但不会自动同步公网版本。只有提交、构建配置和服务环境一致时，行为才可直接对照。官网首页可以本地预览，登录、订阅等功能仍需要对应服务环境变量。

在仓库根目录运行一次 `bun install`，然后选择需要的入口：

| 命令 | 用途 |
| --- | --- |
| `bun run dev:website` | 启动官网（需要相应服务配置） |
| `bun run dev:docs` | 启动文档站 |
| `bun run dev:desktop` | 启动桌面端 |
| `bun run dev:app` | 单独调试工作台界面；后端需要另外启动 |
| `bun run build:website` | 构建官网 |
| `bun run build:docs` | 构建文档站 |
| `bun run build:desktop` | 构建桌面端代码 |

为兼容原有使用习惯，`dev:web` 仍然启动工作台界面，`dev:console` 是 `dev:website` 的别名。
桌面安装包在 `packages/desktop-app` 中运行 `bun run package:win` 等对应平台命令生成。

## 影响范围

本次只调整源码目录和引用路径，保留 npm 包名与共享依赖关系，不修改用户数据目录、数据库、域名或应用身份。已经安装的桌面端和已部署的网站不因本地文件移动而改变。

这不是两个可单独复制的仓库：两端仍然依赖根目录的 Bun workspace、锁文件和公共包。首次获取本次调整后需要在根目录运行 `bun install` 更新工作区链接。

仓库内的 CI、SST 部署路径已同步。若托管平台控制台另存了旧 Root Directory（例如 `packages/console/app`），下次部署前需改为 `packages/website/app`；本地 IDE、外部脚本和文件夹收藏也需要更新。发布脚本保留旧路径用于检索迁移前的 Git 历史。

## 本次验证

- 两个 Windows 启动器均已实际运行验证：官网首页显示正常、浏览器无脚本错误；桌面窗口已出现，内置服务报告就绪。这是开发模式验证，不代表下述网站生产构建问题已修复。

- 官网与桌面端类型检查通过。
- 桌面端完整构建、安装包输入检查通过；工作台浏览器版构建通过。
- 翻译脚本的 12 项测试通过，冻结锁文件安装通过。
- 官网构建在当前 Windows 环境被 SolidStart 依赖生成的未转义反斜杠路径阻塞；出错路径是第三方依赖内部路径，与移动的业务目录无关。
- 文档站构建被 `src/components/Lander.astro` 中的顶层 `return` 阻塞。这段代码已存在于移动前的本地修改中，本次保留原样。

因此桌面端的构建验证已通过，网站侧还不能宣称完整构建通过。上述两个构建问题需另行修复后再发布。
