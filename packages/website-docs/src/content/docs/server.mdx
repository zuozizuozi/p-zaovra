---
title: Server
description: Interact with zaovra server over HTTP.
---

import config from "../../../config.mjs"
export const typesUrl = `${config.github}/blob/dev/packages/sdk/js/src/v2/gen/types.gen.ts`

The `zaovra serve` command runs a headless HTTP server that exposes an OpenAPI endpoint that an zaovra client can use.

---

### Usage

```bash
zaovra serve [--port <number>] [--hostname <string>] [--cors <origin>]
```

#### Options

| Flag            | Description                         | Default          |
| --------------- | ----------------------------------- | ---------------- |
| `--port`        | Port to listen on                   | `4096`           |
| `--hostname`    | Hostname to listen on               | `127.0.0.1`      |
| `--mdns`        | Enable mDNS discovery               | `false`          |
| `--mdns-domain` | Custom domain name for mDNS service | `zaovra.local` |
| `--cors`        | Additional browser origins to allow | `[]`             |

`--cors` can be passed multiple times:

```bash
zaovra serve --cors http://localhost:5173 --cors https://app.example.com
```

---

### Authentication

Set `ZAOVRA_SERVER_PASSWORD` to protect the server with HTTP basic auth. The username defaults to `zaovra`, or set `ZAOVRA_SERVER_USERNAME` to override it. This applies to both `zaovra serve` and `zaovra web`.

```bash
ZAOVRA_SERVER_PASSWORD=your-password zaovra serve
```

---

### How it works

When you run `zaovra` it starts a TUI and a server. Where the TUI is the
client that talks to the server. The server exposes an OpenAPI 3.1 spec
endpoint. This endpoint is also used to generate an [SDK](/docs/sdk).

:::tip
Use the zaovra server to interact with zaovra programmatically.
:::

This architecture lets zaovra support multiple clients and allows you to interact with zaovra programmatically.

You can run `zaovra serve` to start a standalone server. If you have the
zaovra TUI running, `zaovra serve` will start a new server.

---

#### Connect to an existing server

When you start the TUI it randomly assigns a port and hostname. You can instead pass in the `--hostname` and `--port` [flags](/docs/cli). Then use this to connect to its server.

The [`/tui`](#tui) endpoint can be used to drive the TUI through the server. For example, you can prefill or run a prompt. This setup is used by the Zaovra [IDE](/docs/ide) plugins.

---

## Spec

The server publishes an OpenAPI 3.1 spec that can be viewed at:

```
http://<hostname>:<port>/doc
```

For example, `http://localhost:4096/doc`. Use the spec to generate clients or inspect request and response types. Or view it in a Swagger explorer.

---

## APIs

The zaovra server exposes the following APIs.

---

### Global

| Method | Path             | Description                    | Response                             |
| ------ | ---------------- | ------------------------------ | ------------------------------------ |
| `GET`  | `/global/health` | Get server health and version  | `{ healthy: true, version: string }` |
| `GET`  | `/global/event`  | Get global events (SSE stream) | Event stream                         |

---

### Project

| Method | Path               | Description             | Response                                      |
| ------ | ------------------ | ----------------------- | --------------------------------------------- |
| `GET`  | `/project`         | List all projects       | <a href={typesUrl}><code>Project[]</code></a> |
| `GET`  | `/project/current` | Get the current project | <a href={typesUrl}><code>Project</code></a>   |

---

### Path & VCS

| Method | Path    | Description                          | Response                                    |
| ------ | ------- | ------------------------------------ | ------------------------------------------- |
| `GET`  | `/path` | Get the current path                 | <a href={typesUrl}><code>Path</code></a>    |
| `GET`  | `/vcs`  | Get VCS info for the current project | <a href={typesUrl}><code>VcsInfo</code></a> |

---

### Instance

| Method | Path                | Description                  | Response  |
| ------ | ------------------- | ---------------------------- | --------- |
| `POST` | `/instance/dispose` | Dispose the current instance | `boolean` |

---

### Config

| Method  | Path                | Description                       | Response                                                                                 |
| ------- | ------------------- | --------------------------------- | ---------------------------------------------------------------------------------------- |
| `GET`   | `/config`           | Get config info                   | <a href={typesUrl}><code>Config</code></a>                                               |
| `PATCH` | `/config`           | Update config                     | <a href={typesUrl}><code>Config</code></a>                                               |
| `GET`   | `/config/providers` | List providers and default models | `{ providers: `<a href={typesUrl}>Provider[]</a>`, default: { [key: string]: string } }` |

---

### Integrations and credentials

Provider and MCP authentication use the V2 Integration and Credential Store. The removed `/provider/auth` and `/auth/:providerID` routes are not compatibility aliases.

| Method   | Path                                              | Description                         |
| -------- | ------------------------------------------------- | ----------------------------------- |
| `GET`    | `/api/integration`                                | List available integrations         |
| `GET`    | `/api/integration/:integrationID`                 | Get integration details             |
| `POST`   | `/api/integration/:integrationID/connect/key`     | Store a key credential              |
| `POST`   | `/api/integration/:integrationID/connect/oauth`   | Start OAuth                         |
| `GET`    | `/api/integration/attempt/:attemptID`             | Read OAuth attempt state            |
| `POST`   | `/api/integration/attempt/:attemptID/complete`    | Complete an OAuth attempt           |
| `DELETE` | `/api/integration/attempt/:attemptID`             | Cancel an OAuth attempt             |
| `DELETE` | `/api/credential/:credentialID`                   | Remove a credential                 |

---

### SessionV2

SessionV2 is the only Agent execution API. Prompt admission is durable and separate from execution; `steer` is the default delivery mode and `queue` is explicit.

| Method   | Path                                            | Description                                  |
| -------- | ----------------------------------------------- | -------------------------------------------- |
| `GET`    | `/api/session`                                  | List projected sessions                      |
| `POST`   | `/api/session`                                  | Create a durable session                     |
| `GET`    | `/api/session/active`                           | List process-local active drains             |
| `GET`    | `/api/session/:sessionID`                       | Get a session                                |
| `PATCH`  | `/api/session/:sessionID`                       | Update title or archive state                |
| `DELETE` | `/api/session/:sessionID`                       | Delete a session                             |
| `POST`   | `/api/session/:sessionID/prompt`                | Admit a durable steer or queue input         |
| `POST`   | `/api/session/:sessionID/interrupt`             | Interrupt active process-local execution     |
| `POST`   | `/api/session/:sessionID/wait`                  | Wait for the current local drain             |
| `POST`   | `/api/session/:sessionID/compact`               | Run the checkpointed compaction state machine |
| `POST`   | `/api/session/:sessionID/shell`                 | Run and persist one explicit user command    |
| `POST`   | `/api/session/:sessionID/agent`                 | Switch the selected Agent                    |
| `POST`   | `/api/session/:sessionID/model`                 | Switch the selected model                    |
| `GET`    | `/api/session/:sessionID/todo`                  | Read durable todos                           |
| `GET`    | `/api/session/:sessionID/input/pending`         | Read admitted inputs awaiting consumption    |

---

### Session history, approvals, and recovery

| Method | Path                                                    | Description                         |
| ------ | ------------------------------------------------------- | ----------------------------------- |
| `GET`  | `/api/session/:sessionID/message`                        | List projected V2 messages          |
| `GET`  | `/api/session/:sessionID/message/:messageID`             | Get one projected message           |
| `GET`  | `/api/session/:sessionID/history`                        | Read durable Session events         |
| `GET`  | `/api/session/:sessionID/event`                          | Tail durable Session events         |
| `GET`  | `/api/session/:sessionID/permission`                     | List pending permission requests    |
| `POST` | `/api/session/:sessionID/permission/:requestID/reply`    | Reply to a permission request       |
| `GET`  | `/api/session/:sessionID/question`                       | List pending questions              |
| `POST` | `/api/session/:sessionID/question/:requestID/reply`      | Answer a question                   |
| `POST` | `/api/session/:sessionID/question/:requestID/reject`     | Reject a question                   |
| `POST` | `/api/session/:sessionID/revert/stage`                    | Stage a message/file revert         |
| `POST` | `/api/session/:sessionID/revert/clear`                    | Clear a staged revert               |
| `POST` | `/api/session/:sessionID/revert/commit`                   | Commit a staged revert              |

---

### Files

| Method | Path                     | Description                        | Response                                                                                    |
| ------ | ------------------------ | ---------------------------------- | ------------------------------------------------------------------------------------------- |
| `GET`  | `/find?pattern=<pat>`    | Search for text in files           | Array of match objects with `path`, `lines`, `line_number`, `absolute_offset`, `submatches` |
| `GET`  | `/find/file?query=<q>`   | Find files and directories by name | `string[]` (paths)                                                                          |
| `GET`  | `/find/symbol?query=<q>` | Find workspace symbols             | <a href={typesUrl}><code>Symbol[]</code></a>                                                |
| `GET`  | `/file?path=<path>`      | List files and directories         | <a href={typesUrl}><code>FileNode[]</code></a>                                              |
| `GET`  | `/file/content?path=<p>` | Read a file                        | <a href={typesUrl}><code>FileContent</code></a>                                             |
| `GET`  | `/file/status`           | Get status for tracked files       | <a href={typesUrl}><code>File[]</code></a>                                                  |

#### `/find/file` query parameters

- `query` (required) — search string (fuzzy match)
- `type` (optional) — limit results to `"file"` or `"directory"`
- `directory` (optional) — override the project root for the search
- `limit` (optional) — max results (1–200)
- `dirs` (optional) — legacy flag (`"false"` returns only files)

---

### MCP V2

| Method | Path                        | Description                         |
| ------ | --------------------------- | ----------------------------------- |
| `GET`  | `/api/mcp`                  | Read canonical Core MCP status      |
| `GET`  | `/api/mcp/resources`        | List MCP resources                  |
| `POST` | `/api/mcp/:name/connect`    | Connect one configured MCP server   |
| `POST` | `/api/mcp/:name/disconnect` | Disconnect one MCP server           |

---

### Agents

| Method | Path     | Description               | Response                                    |
| ------ | -------- | ------------------------- | ------------------------------------------- |
| `GET`  | `/agent` | List all available agents | <a href={typesUrl}><code>Agent[]</code></a> |

---

### Logging

| Method | Path   | Description                                                  | Response  |
| ------ | ------ | ------------------------------------------------------------ | --------- |
| `POST` | `/log` | Write log entry. Body: `{ service, level, message, extra? }` | `boolean` |

---

### TUI

| Method | Path                    | Description                                 | Response               |
| ------ | ----------------------- | ------------------------------------------- | ---------------------- |
| `POST` | `/tui/append-prompt`    | Append text to the prompt                   | `boolean`              |
| `POST` | `/tui/open-help`        | Open the help dialog                        | `boolean`              |
| `POST` | `/tui/open-sessions`    | Open the session selector                   | `boolean`              |
| `POST` | `/tui/open-themes`      | Open the theme selector                     | `boolean`              |
| `POST` | `/tui/open-models`      | Open the model selector                     | `boolean`              |
| `POST` | `/tui/submit-prompt`    | Submit the current prompt                   | `boolean`              |
| `POST` | `/tui/clear-prompt`     | Clear the prompt                            | `boolean`              |
| `POST` | `/tui/execute-command`  | Execute a command (`{ command }`)           | `boolean`              |
| `POST` | `/tui/show-toast`       | Show toast (`{ title?, message, variant }`) | `boolean`              |
| `GET`  | `/tui/control/next`     | Wait for the next control request           | Control request object |
| `POST` | `/tui/control/response` | Respond to a control request (`{ body }`)   | `boolean`              |

---

### Auth

| Method | Path        | Description                                                     | Response  |
| ------ | ----------- | --------------------------------------------------------------- | --------- |
| `PUT`  | `/auth/:id` | Set authentication credentials. Body must match provider schema | `boolean` |

---

### Events

| Method | Path     | Description                                                                   | Response                  |
| ------ | -------- | ----------------------------------------------------------------------------- | ------------------------- |
| `GET`  | `/event` | Server-sent events stream. First event is `server.connected`, then bus events | Server-sent events stream |

---

### Docs

| Method | Path   | Description               | Response                    |
| ------ | ------ | ------------------------- | --------------------------- |
| `GET`  | `/doc` | OpenAPI 3.1 specification | HTML page with OpenAPI spec |
