# LocalFlow specification

- Target: `.local-flow`; interaction: scroll reveal and low-amplitude line pulses.
- Desktop reference: 128px vertical padding and roughly 1104px content rail; centered 650px statement, then 1000×500 architecture diagram.
- Zaovra copy: “Everything connected. / Own the workflow.”
- Diagram nodes: Workspace → Context graph → Zaovra runtime → model/provider → reviewable diff. Cloud/vendor lock-in paths are visually severed; local filesystem and tools remain connected.
- Electric green may appear only as a status signal; the overall palette remains Zaovra blue/navy.
- Mobile: diagram reflows vertically instead of shrinking into illegibility.
