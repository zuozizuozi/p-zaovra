# Capabilities specification

- Target: `.capabilities` + `.stats-strip`; interaction: scroll stagger and hover border response.
- Desktop reference: centered title, 3×2 cards at roughly 368×225, 24px gaps; four equal 300×156 stats below.
- Cards: Understands architecture; Coordinates agents; Uses terminal + tools; Keeps sessions durable; Connects any model; Produces reviewable diffs.
- Stats should be factual product descriptors, not invented performance claims: Open source, Local context, Multi-model, Cross-platform.
- Mobile: one card per row and 2×2 stats; compact copy.
