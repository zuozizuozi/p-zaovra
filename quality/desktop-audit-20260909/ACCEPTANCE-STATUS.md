# 当前交付状态：可进入人工测试

用户已明确本次范围是“修复到可用，交给用户人工测试”，不要求现在打包或正式发布。因此签名、安装升级、自动化原生确认不再作为本次交付阻塞。当前开发入口及测试顺序见 MANUAL-HANDOFF.md；下文保留此前发布验收证据边界，不能用其旧阻塞结论覆盖最新交付范围。

整体目标未完成。以下区分已有修复证据和缺失的交付证据；初次审计行为见 REPORT.md，逐批原始结果见 REPAIR-PROGRESS.md。官方 API 未接入不列为缺陷。

| 范围 / 问题编号 | 已有证据 | 当前限制 |
| --- | --- | --- |
| 审批、提问、拒绝、跳过（1、32、33） | 打包双窗口工具调用与回复、拒绝后再发送、wait 204；102–107、123批 | 模型与账户为fixture |
| 窗口权限（2） | 13批打包双窗口权限检查 | 不替代系统通知全部投递场景 |
| 撤销、历史、轮次差异、项目目录（3、4、7、8、15、30、31） | 分页/store/HTTP回归；95–98、110、123批真实文件与审查 | 长历史和磁盘退出是不同验收项 |
| 队列与重试（5、6、14、37） | 111–122批steer/queue、取消、编辑、立即发送、请求/响应丢失实测 | 全进程退出后排队恢复尚无组合验收 |
| 命令与子任务（9、12、16） | 10–14批实际TaskTool/Core/HTTP与打包子会话链路 | 远程模型行为不是fixture证据范围 |
| 协议链接、翻译（10、11） | 首批解析修复与8批语言键/占位符回归 | OS协议注册与实际安装冷启动仍需安装验收 |
| 配置、模型、运行实例（13、17、18、19、20） | 15–71批配置保存、缓存、文件工具、格式化/LSP/PTY实机证据 | 正式环境凭据与组织服务未替代验证 |
| WSL生命周期（22、24） | 78–83批真实Ubuntu进程/任务停止；85–88批选择与离线恢复 | 不声称所有发行版均测试 |
| 退出与重启控制（23） | 76–82批清理/安装失败控制与Electron事件验证 | 全产品退出恢复仍待明确重启允许 |
| 标签、凭据、浮层、多窗口存储（25–29、35） | 85–94批动态浏览器/Electron与112批真实设置下拉框 | 各测试的过程退出/renderer退出边界仍适用 |
| 流式文本恢复（34、36） | 109–118批作用域ID、活动快照、offset及双窗口刷新后继续输出 | 活动内存快照不等同于崩溃后持久内容 |
| 发布签名与升级（21） | 99–100批配置、签名拒绝、NSIS元数据校验；124批当前产物检查 | 当前包NotSigned，本机无签名条件，仓库releases为空 |

## 尚未取得通过证据的门槛

1. **整个桌面退出再打开（已通过）**：用户明确批准后，第128批通过真实 app.relaunch 执行，旧PID34576和旧服务端口退出，新PID65956从同一profile恢复。process-recovery-result-123.json证明会话context、tabs、未发送草稿及文件内容一致。验证只注入账户fixture，不重建业务数据；含一次用于加载账户fixture的renderer reload，不声称无fixture的登录冷启动验收。
2. **真实签名安装、升级、卸载与OS协议入口**：正式签名条件/产物尚不可用，不能把未签名win-unpacked或旧测试NSIS视为完成。
3. **原生项目选择全流程**：128批发现并修复第38项：新版布局未注册project.open，导致桌面菜单禁用、Ctrl+O无效。最新包真实菜单已启用，原生Windows对话框已导航到中文带空格目录。工具点击/设置值失败（坐标不可用、UIA CacheRequest错误），键盘导航成功；最终确认选择及目录/草稿校验尚待完成，不把打开对话框当作全流程通过。
4. **账户与远程供应商完整业务**：现有组合桌面验证明确使用账户/模型fixture。官方API接入由用户另行处理；这里仅记录验证边界，不新增API未接入缺陷。

## 本轮已关闭的测试缺口

录制 HTTP runner 回归挂起已定位并修复：测试使用真实 PluginV2，却未登记插件初始化完成标记，执行器停在 plugins.wait，尚未进入 HTTP 回放。第126批补齐测试初始化后，原始测试通过（1 pass、5断言，约1.3秒），Core类型检查通过。证据：recorded-runner-fixed-126.txt、recorded-runner-types-126.txt。属于测试夹具修复，未修改产品执行逻辑。

最新打包产物：package-project-open-128/win-unpacked/Zaovra Beta.exe（main-DDUuWgaW.js）。
独立磁盘验收准备：process-recovery-manifest-123.json。
本表不是完整通过声明。正式签名安装升级仍需要真实签名条件或产物。原生工具本轮已可调用，但系统对话框的确认操作仍未完成；已请求用户完成该点击。账户与模型fixture边界不变。
